#!/bin/env sh

# start MPD if not already running and monitors it's state (to e.g. update statusbar info)

sig=7 # the signal expected by i3blocks

pgrep mpd > /dev/null || mpd

while true; do
    mpc idle player > /dev/null && sleep 0.01 && pkill -RTMIN+"$sig" i3blocks
done
